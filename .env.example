# ===========================================
# Конфигурация окружения PHP-Httpd-Socket
# ===========================================

# Настройки базы данных MySQL
MYSQL_ROOT_PASSWORD=secret-root-password
MYSQL_DATABASE=app
MYSQL_USER=app
MYSQL_PASSWORD=app

# Настройки phpMyAdmin
PMA_HOST=mysql-httpd-socket
PMA_ARBITRARY=1

# Xdebug (необязательно; по умолчанию включается через docker-compose.xdebug.yml при использовании make xdebug-up)
# Устанавливайте эти значения, только если хотите включить Xdebug без оверлей-файла compose
XDEBUG_MODE=
XDEBUG_START=
XDEBUG_CLIENT_HOST=host.docker.internal